services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: jbrealty
      POSTGRES_PASSWORD: jbrealty
      POSTGRES_DB: jbrealty
    ports:
      - "5432:5432"
    volumes:
      - jbrealty_pg:/var/lib/postgresql/data

volumes:
  jbrealty_pg:
